function color_matrix = SingleColorMap(color,number_of_colors)

cix = {'k',1:3;
    'r',1;
    'g',2;
    'b',3;
    'y',[1:2];
    'm',[1,3];
    'c',[2:3]};

ismember(cix(:,1),color)
    
    

%number of colors limited to 6

d = 1/number_of_colors;
for i = 1:number_of_colors
    cols(:,ic)=
end



color_matrix = cols;
